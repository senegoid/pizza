# Primeiro teste do servidor com sequelize, sqlite e graphql

```bash
md server
cd server
yarn init --yes
yarn add apollo-server graphql
yarn add sequelize sequelize-cli sqlite3
yarn sequelize init
```